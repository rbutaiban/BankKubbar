import { FilterAmountPipe } from './filter-amount.pipe';

describe('FilterAmountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAmountPipe();
    expect(pipe).toBeTruthy();
  });
});
